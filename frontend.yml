- name: frontend setup
  hosts: all
  become: true
  tasks:
    - name: Install Nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest
    - name: Start service nginx
      ansible.builtin.service:
        name: nginx
        state: started

